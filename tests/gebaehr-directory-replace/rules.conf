create repository git-repo
end repository

match /trunk/
  repository git-repo
  branch master
end match

match /branches/([^/]+)/
  repository git-repo
  branch \1
end match

match /branches/
  action recurse
end match
